# FastAPI and web framework dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Template engine
jinja2==3.1.2

# Forms and file handling
python-multipart==0.0.6

# MongoDB driver
motor==3.3.2
pymongo==4.6.0

# Environment variable management
python-dotenv==1.0.0

# Date and time handling (included in Python standard library)
# datetime - built-in

# Additional utilities
bson==0.5.10  # For ObjectId handling (usually comes with pymongo)

# Development dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2  # For testing FastAPI endpoints

# Production server (alternative to uvicorn)
gunicorn==21.2.0

# Logging and monitoring (optional)
structlog==23.2.0

# Data validation (optional, FastAPI uses pydantic internally)
pydantic==2.5.0